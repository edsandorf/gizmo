#   Change some overall options
options(prompt = "R> ", continue = " ", digits = 6)

#   Load packages used for development 
 if(interactive()){
    suppressMessages(lapply(c("devtools", "roxygen2", "testthat", "knitr")))
 }
